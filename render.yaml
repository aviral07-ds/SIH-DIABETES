services:
  - type: web
    name: sih-diabetes-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
